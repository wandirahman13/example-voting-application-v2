apiVersion: v1
Kind: Service
metadata:
  name: db
  labels:
    name: db-service
    app: demo-voting-app

spec:
  selector:
    name: postgres-pod
    app: demo-voting-app
  ports:
    - port: 5432
      targetPort: 5432